# AGENTS.md

This repository's guidance for coding agents lives in [`CLAUDE.md`](CLAUDE.md). Read it first — it
covers the architecture, the build/test commands, the Dependency Rule, and the git/commit
conventions.

Decision records are in [`docs/adr/`](docs/adr/README.md); read the relevant ADR before changing
anything it governs.
